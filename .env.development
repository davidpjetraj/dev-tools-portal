# =============================================================
# Dev Tools Portal — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# =============================================================

# ---- API ----
NODE_ENV=development
PORT=8080

# MongoDB connection string
MONGODB_URI=mongodb://mongo:27017/dev-tools-portal

# JWT secret — use a long, random string in production
JWT_SECRET=change-me-to-a-long-random-secret

# Admin credentials (used for initial seeded admin account)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# ---- Web ----
# URL of the GraphQL API (used by the Vite dev server proxy & Docker)
VITE_API_URL=http://localhost:8080/graphql

# ---- CORS ----
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000
